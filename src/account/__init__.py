"""
Accounts: sign-in, uploads, per-account estimates and insights.

Everything here is optional at runtime. With the Supabase environment
variables unset, the public page and its API behave exactly as before and
the /api/* routes answer 503.
"""
